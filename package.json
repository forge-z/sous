{
  "name": "sous-lite",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22.16.0 <27" },
  "scripts": {
    "dev": "vite --port 5173",
    "start": "node server/index.js",
    "build": "vite build",
    "test": "node --test test/parser.check.js test/api.check.js",
    "check": "npm test && npm run build"
  },
  "dependencies": {
    "@fastify/static": "10.1.3",
    "fastify": "^5.2.1"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
